import SwiftUI

// Deprecated: No longer used. Usage is fetched directly via Anthropic API.
// Kept for compatibility.
struct LoginWebView: View {
    var body: some View {
        EmptyView()
    }
}

struct LoginSheet: View {
    var body: some View {
        EmptyView()
    }
}
